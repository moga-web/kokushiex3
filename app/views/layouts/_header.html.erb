<div class="flex flex-wrap p-2 flex-col md:flex-row items-center bg-sky-100">
    <a class="flex title-font font-medium items-center text-gray-900 mb-2 md:mb-0">
        <%= image_tag 'logo.png', size: 50 %>
        <span class="ml-3 text-xl">国試ex for PT</span>
    </a>
    <nav class="md:ml-auto flex flex-wrap items-center text-base justify-center">
        <div class="mr-5 hover:text-orange-300">
            <%= link_to 'トップ', root_path %>
        </div>
        <div class="mr-5 hover:text-orange-300">
            <%= link_to 'ダッシュボード', dashboard_path %>
        </div>
        <!-- パスができたら修正 -->
        <div class="mr-5 hover:text-orange-300">
            <%= link_to '過去問演習', tests_select_path %>
        </div>
        <!-- パスができたら修正 -->
        <% if user_signed_in? %>
            <div class="mr-5 hover:text-orange-300">
                <%= button_to 'ログアウト', destroy_user_session_path, method: :delete, data: { turbo_method: :delete } %>
            </div>
            <div class="mr-5 hover:text-orange-300">
                <%= link_to 'マイページ', account_path %>
            </div>
        <% else %>
            <div class="mr-5 hover:text-orange-300">
                <%= link_to 'ログイン', new_user_session_path %>
            </div>
        <% end %>
    </nav>
</div>
